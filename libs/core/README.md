# Core Library

Shared utilities and domain models consumed by multiple services will live here. The folder currently acts as a placeholder until the first shared package is extracted from the API service.
